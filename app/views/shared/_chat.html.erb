<div class="chat-container">
  <div class="chat-box" id="collapseChat">
    <h1>Nomad Chat</h1>
    <div>
      <% Chatroom.find_by(name: "general").messages.includes(:user).each do |message| %>
        <%= render partial: "messages/message", locals: { message: message } %>
      <% end %>
    </div>
    <%= simple_form_for [ Chatroom.find_by(name: "general"), Message.new ], remote: true, html: { class: "message_submit"} do |f| %>
      <%= f.input :content, label: false, placeholder: "Message ##{Chatroom.find_by(name: "general").name}" %>
    <% end %>
  </div>
  <button class="btn btn-primary chat-widget" type="button" data-toggle="collapse" data-target="#collapseChat" aria-expanded="false" aria-controls="collapseChat">
      Chat
  </button>
</div>
