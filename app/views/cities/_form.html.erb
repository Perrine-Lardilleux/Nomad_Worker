<%= simple_form_for([ @city, @review ], remote: true, html: {class:"form-label"}) do |f| %>
  <%= f.input :content, label: 'Leave a review 🌟', as: :text, input_html: {class:"form-review"}, style:"border-radius: 40px;" %>
  <%= f.button :submit, class:"btn-round", value: 'Add review' %>
<% end %>
